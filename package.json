{
  "name": "futurecheck-mvp",
  "version": "1.0.0",
  "description": "FutureCheck Pro - Digital Services Platform",
  "main": "server/index.js",
  "scripts": {
    "start": "NODE_ENV=production node server/index.js",
    "dev": "NODE_ENV=development nodemon server/index.js",
    "build": "echo 'Static files ready'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "npm install && cp .env.example .env",
    "lint": "echo 'No linting configured'",
    "clean": "rm -rf node_modules package-lock.json && npm install"
  },
  "keywords": [
    "futurecheck",
    "digital",
    "services",
    "stripe",
    "seo",
    "website",
    "business"
  ],
  "author": "Marcel Gärtner <gaertner-marcel@web.de>",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "nodemailer": "^7.0.5",
    "stripe": "^14.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
